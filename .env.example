# Copia questo file in .env.local e inserisci i valori reali.
# Li trovi su Supabase → Project Settings → API.
# .env.local non va mai committato (è in .gitignore).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
